{
  "name": "protopen-docs",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "web:dev": "npm --workspace @protopen/web run dev",
    "web:build": "npm --workspace @protopen/web run build",
    "web:preview": "npm --workspace @protopen/web run preview"
  },
  "devDependencies": {
    "@protolabsai/design": "^0.3.0",
    "@protolabsai/vitepress-theme": "^0.3.0",
    "vitepress": "^1.6.4"
  }
}
